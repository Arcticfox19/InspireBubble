# GitHub Pages 部署指南

## ✅ 已完成的配置

1. **Vite 配置更新**：已设置 `base: '/InspireBubble/'` 路径
2. **GitHub Actions Workflow**：已创建自动部署脚本
3. **构建配置**：使用 `npm run build` 构建项目

## 🚀 部署步骤

### 第一步：启用 GitHub Pages

1. 访问你的仓库：https://github.com/Arcticfox19/InspireBubble
2. 点击 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**（页面）
4. 在 **Source**（源）部分：
   - 选择 **GitHub Actions** 作为部署源
   - （不要选择 branch，选择 GitHub Actions）
5. 点击 **Save**（保存）

### 第二步：推送代码触发部署

将配置推送到 GitHub：

```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push
```

### 第三步：等待部署完成

1. 在仓库页面，点击 **Actions** 标签
2. 查看 **Deploy to GitHub Pages** workflow 的运行状态
3. 等待构建和部署完成（通常需要 1-3 分钟）

### 第四步：访问你的网站

部署完成后，你的网站将可以通过以下地址访问：

**https://arcticfox19.github.io/InspireBubble/**

## 📝 工作流程说明

### 自动部署触发条件

- ✅ 每次推送到 `main` 分支时自动部署
- ✅ 也可以手动触发（在 Actions 页面点击 "Run workflow"）

### 部署流程

1. **构建阶段**：
   - 安装 Node.js 20
   - 安装项目依赖（`npm ci`）
   - 构建项目（`npm run build`）
   - 生成 `dist` 目录

2. **部署阶段**：
   - 将 `dist` 目录上传到 GitHub Pages
   - 自动发布到 `https://arcticfox19.github.io/InspireBubble/`

## 🔧 配置说明

### Vite 配置

```typescript
// vite.config.ts
export default defineConfig({
  base: '/InspireBubble/', // GitHub Pages 基础路径
  // ...
})
```

**重要**：如果仓库名更改，需要同步更新 `vite.config.ts` 中的 `base` 路径。

### GitHub Actions Workflow

工作流文件位置：`.github/workflows/deploy.yml`

- 使用 Node.js 20
- 自动缓存 npm 依赖以加快构建速度
- 支持手动触发部署

## 🐛 常见问题

### 1. 页面显示 404

**原因**：`base` 路径配置不正确

**解决**：
- 检查 `vite.config.ts` 中的 `base` 路径是否与仓库名匹配
- 确保路径格式为：`/仓库名/`（注意前后都有斜杠）

### 2. 资源文件加载失败

**原因**：路径问题

**解决**：
- 确保所有资源路径使用相对路径或配置的 base 路径
- 检查浏览器控制台的错误信息

### 3. 部署失败

**检查清单**：
- ✅ GitHub Pages 设置中选择了 "GitHub Actions" 作为源
- ✅ 仓库有 Pages 写入权限
- ✅ workflow 文件语法正确
- ✅ `package.json` 中有 `build` 脚本

### 4. 如何更新网站

只需推送代码到 `main` 分支，GitHub Actions 会自动重新部署：

```bash
git add .
git commit -m "更新内容"
git push
```

## 📚 相关链接

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html#github-pages)

## 🎉 完成！

配置完成后，你的项目将自动部署到 GitHub Pages，每次代码更新都会自动重新部署。
